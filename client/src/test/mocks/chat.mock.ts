import { rest } from 'msw';

export const chatHandlers = [
  // rest.get('http://localhost:4000/api/v1/message/:id', (req, res, ctx) => {
  //   return res(ctx.status(200), ctx.json(messages));
  // }),
];
